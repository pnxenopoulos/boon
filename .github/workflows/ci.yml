name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ "**" ]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  lint:
    name: Format & Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust (stable) + rustfmt, clippy
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Cache cargo/target
        uses: Swatinem/rust-cache@v2
        with:
          key: lint-${{ runner.os }}-stable

      - name: rustfmt
        run: cargo fmt --all -- --check

      - name: clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: rustdoc
        env:
          RUSTDOCFLAGS: "-D warnings"
        run: cargo doc --workspace --no-deps --all-features

  deny:
    name: Dependency Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: cargo deny
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
          arguments: --all-features

  test-rust:
    name: Build & Test (${{ matrix.os }} / ${{ matrix.rust }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust (${{ matrix.rust }})
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}

      - name: Cache cargo/target
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ runner.os }}-${{ matrix.rust }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: nextest

      - name: Build
        run: cargo build --workspace --all-features --locked --exclude boon-python

      - name: Test
        run: cargo nextest run --workspace --all-features --locked --exclude boon-python --failure-output immediate-final

  test-python:
    name: Python Tests
    runs-on: ubuntu-latest
    env:
      # Space-separated list of public URLs for demo fixture files.
      DEMO_URLS: "https://github.com/pnxenopoulos/boon-fixtures/releases/download/68359505/68359505.dem"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache cargo/target
        uses: Swatinem/rust-cache@v2
        with:
          key: python-${{ runner.os }}-stable

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: 3.13

      - name: Download demo fixtures
        working-directory: crates/boon-python
        run: |
          mkdir -p tests/fixtures
          if [ -n "$DEMO_URLS" ]; then
            for url in $DEMO_URLS; do
              echo "Downloading $url ..."
              curl -fSL --retry 3 -o "tests/fixtures/$(basename "$url")" "$url"
            done
          else
            echo "No DEMO_URLS configured — skipping fixture download"
          fi

      - name: Install dependencies and build wheel
        working-directory: crates/boon-python
        run: |
          uv sync
          uv run maturin develop

      - name: Run pytest
        working-directory: crates/boon-python
        run: uv run pytest tests/ -v

  docs:
    name: Sphinx Docs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Sphinx dependencies
        run: pip install -r crates/boon-python/docs/requirements.txt

      - name: Build docs
        working-directory: crates/boon-python/docs
        run: sphinx-build -W -b html . _build/html

  check:
    name: CI Check
    if: always()
    needs: [lint, deny, test-rust, test-python, docs]
    runs-on: ubuntu-latest
    steps:
      - name: Verify all jobs passed
        run: |
          if [[ "${{ needs.lint.result }}" != "success" ||
                "${{ needs.deny.result }}" != "success" ||
                "${{ needs.test-rust.result }}" != "success" ||
                "${{ needs.test-python.result }}" != "success" ||
                "${{ needs.docs.result }}" != "success" ]]; then
            echo "One or more required jobs failed"
            exit 1
          fi

  coverage:
    name: Coverage
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust (stable) + components (incl. llvm-tools)
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: llvm-tools-preview

      - name: Cache cargo/target
        uses: Swatinem/rust-cache@v2
        with:
          key: coverage-${{ runner.os }}-stable

      - name: Install cargo-nextest & cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: |
            nextest
            cargo-llvm-cov

      - name: Clean coverage artifacts
        run: cargo llvm-cov clean --workspace

      - name: Run coverage and print summary
        run: |
          cargo llvm-cov nextest --workspace --all-features
          echo "------ Coverage Summary ------"
          cargo llvm-cov report --summary-only
